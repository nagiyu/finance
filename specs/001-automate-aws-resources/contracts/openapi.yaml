openapi: 3.0.1
info:
  title: Finance Client API (簡易)
  version: 1.0.0
  description: "Client 用の公開エンドポイント（ヘルスチェック等の最小定義）"
servers:
  - url: https://{host}
    variables:
      host:
        default: your-cloudfront-domain.example.com
paths:
  /health:
    get:
      summary: ヘルスチェック
      responses:
        '200':
          description: OK
          content:
            text/plain:
              schema:
                type: string
                example: OK
